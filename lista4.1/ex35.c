#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>



int main(){
    int vet[10];
    int vet_ordenado[10]
    int i,n,j;
    bool posicao = true;
    int lista_foram[10]; 


    for(i=0;i<10;i++){
        printf("Digite um valor para o vetor na posição %d",i);
        scanf("%d",&vet[i]);
    }
    j = 0;
    menor = vet[0]
    while(j<=9){
        for(i=0;i<10;i++){
            for(n=0;n<10;n++){
                if(vet[n]<vet[i] && n!=i){
                    menor = vet[i]
                } else if(vet[n]==vet[i] && n!=i){
                    menor = vet[i]
                }
            }
        }
    }
}